<form method="post" action="{{ path('team_participants_delete', {'id': team_participant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ team_participant.id) }}">
    <button class="btn">Delete</button>
</form>