{% extends 'base.html.twig' %}

{% block title %}TeamParticipants{% endblock %}

{% block body %}
    <h1>TeamParticipants</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ team_participant.id }}</td>
            </tr>
            <tr>
                <th>Team_id</th>
                <td>{{ team_participant.team_id }}</td>
            </tr>
            <tr>
                <th>User_id</th>
                <td>{{ team_participant.user_id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('team_participants_index') }}">back to list</a>

    <a href="{{ path('team_participants_edit', {'id': team_participant.id}) }}">edit</a>

    {{ include('team_participants/_delete_form.html.twig') }}
{% endblock %}