{% extends 'base.html.twig' %}

{% block title %}Team{% endblock %}

{% block body %}
    <h1>Team</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ team.id }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ team.created_at ? team.created_at|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Draw_at</th>
                <td>{{ team.draw_at ? team.draw_at|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>End_at</th>
                <td>{{ team.end_at ? team.end_at|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Price_min</th>
                <td>{{ team.price_min }}</td>
            </tr>
            <tr>
                <th>Price_max</th>
                <td>{{ team.price_max }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ team.name }}</td>
            </tr>
            <tr>
                <th>Team_code</th>
                <td>{{ team.team_code }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('team_index') }}">back to list</a>

    <a href="{{ path('team_edit', {'id': team.id}) }}">edit</a>

    {{ include('team/_delete_form.html.twig') }}
{% endblock %}