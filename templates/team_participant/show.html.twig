{% extends 'base.html.twig' %}

{% block title %}TeamParticipant{% endblock %}

{% block body %}
    <h1>TeamParticipant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ team_participant.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('team_participant_index') }}">back to list</a>

    <a href="{{ path('team_participant_edit', {'id': team_participant.id}) }}">edit</a>

    {{ include('team_participant/_delete_form.html.twig') }}
{% endblock %}